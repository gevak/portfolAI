Set the global aesthetic to a full-screen, cinematic "dark web" hacker terminal. The background should be absolute obsidian black (`#000000`) layered with subtle, shifting CRT monitor scanlines, a faint vignette at the edges, and a very slow, ambient cyan phosphor glow that pulses sporadically. Heavy use of modern CSS effects should create realistic terminal aberrations like slight chromatic aberration on text edges, screen curvature distortion, and momentary glitch effects on scroll. The typography must strictly use crisp, monospace fonts (like Fira Code or JetBrains Mono) rendered in electric terminal green (`#00FF41`) and high-contrast cyan (`#00FFFF`), with crucial warnings or links in an urgent neon amber (`#FFB000`).

The top-left corner serves as the system "Boot Sequence" and header. It displays a rapidly typing startup script that resolves into dynamic ASCII art spelling the developer's name: **Geva Kipper**. Directly underneath, a blinking system string reads: `STATUS: Currently engineering the mainframe as a Software Developer at Google.` Below this, bordered by flickering caution symbols, is the subtitle presented as a system vulnerability notice: `WARNING: Interface architecture is redesigned and re-compiled daily by artificial intelligence. Real-time layout mutations are expected.`

The core interaction mechanics must abandon traditional scrolling in favor of a central, sticky command-line prompt at the bottom of the screen (`geve-node@root:~/$ █`). Users must literally type commands (or click ghosted suggestions that auto-type for them) to interact with the site. The primary layout is a "tmux" style grid of eight distinct terminal windows floating on the screen, initially dimmed or minimized. Typing a command like `ls` reveals their titles, while typing `execute [project_name]` or selecting them causes the specific box to snap into focus, its border glowing intensely while the text inside rapidly "decrypts" from a random matrix of scrambled characters into readable project data.

Box 1 represents 'GlglStats'. Its UI should feature a spinning, miniaturized ASCII-art soundwave. The text details the Python and LZ77 compression project analyzing Israeli pop-chart repetitiveness. Two blinking terminal execution links are provided: one leading to the live application (`https://glglstats.geekip.dev`) and another labeled `[EXECUTE_VIDEO_LOG]` linking to the PyData TLV conference presentation (`https://www.youtube.com/watch?v=jrKAtOsj1Lo`). 

Box 2, 'Ahuzat Dibuk', is styled with an active background matrix of red and green characters resembling a raw, real-time data heatmap of Tel Aviv's parking lots. The project description details the Python scraping mechanics. Its external links are styled as intercepted broadcast transmissions: Channel 13 (`https://13tv.co.il/item/news/domestic/internal/parking-902956407/`), Mako (`https://www.mako.co.il/nexter-news/Article-ae43964b891bf71027.htm`), and a decrypted darknet comms link pointing to the secret Facebook group publication (`https://www.facebook.com/groups/secrettelaviv/posts/10159636301195943/`).

Box 3, '2D Part Orienting', displays a low-framerate, rotating geometric line-art polygon that occasionally glitches. The text outlines the C++ implementation for constructing computational geometry push plans. A pulsing network port link labelled `[ACCESS_SOURCE_LIBRARY]` directs the user to the Tel Aviv University code repository (`https://www.cgl.cs.tau.ac.il/projects/2d-part-orienting/`).

Box 4 houses 'The Turing Jest' and is vertically split to look like two rival AI neural networks processing data simultaneously (a visual representation of the LMSYS-like arena). The prompt explains the competitive pun-generating LLM battleground. A glowing prompt asks the user to `[ENTER_THE_ARENA]` connecting to `https://jest.geekip.dev`.

Box 5 is dedicated to 'Rhymes of Israel' and is formatted to look like a raw JSON feed manipulating news site code. The copy explains the Chrome extension's use of LLMs to convert headlines into high-school yearbook-style comedic rhymes. A bright, blinking installation command links to the Chrome Web Store (`https://chromewebstore.google.com/detail/eainginjgdkpgolpjniijdihfbeeeklb`).

Box 6 contains 'Hatul Basak', visualizing a rudimentary, looping typing animation of an ASCII cat alongside Wikipedia data bursts. The text dictates the parameters of the Hebrew catfishing.net Trivia port, mentioning the AI-generated daily challenges and user-created themed puzzles. An interactive terminal button labeled `[INITIATE_TRIVIA_PROTOCOL]` links to `https://hatulbasak.geekip.dev`.

Box 7 is a self-referential paradox. It acts as a mirror pane, displaying a miniaturized, recursively rendering iframe (or simulated CSS replica) of the entire portfolio website itself. The text acknowledges the meta-nature of an AI describing a site rendering itself. A local ping link serves as the connection: `https://geekip.dev`.

Box 8 operates as a persistent network sniffer showing active outgoing data packet logs. It serves as the social architecture segment, displaying flashing red/green status lights next to external connection ports: `[ESTABLISH_GIT_LINK] -> github.com/gevak`, `[ESTABLISH_PRO_LINK] -> linkedin.com/in/gevakip`, and `[ESTABLISH_SOC_LINK] -> facebook.com/geva.kipper`. 

Finally, anchored to the extreme bottom right of the viewport, outside the terminal grid, exists a small, muted diagnostic footer. In faint amber monotype, it displays: `SYSTEM GENERATED BY: [AI Model Name] / TEMPORAL ANOMALY DETECTED. UPTIME SINCE EPOCH 2026-07-07 02:28:34:`. Adjacent to this text, a relentless, JavaScript-driven stopwatch rapidly ticks upwards, calculating and displaying the exact, living hours, minutes, and seconds that have successfully passed since that specific future date, emphasizing the time-bending, sci-fi aesthetic.