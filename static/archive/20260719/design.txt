The overall aesthetic of the website is a modern, polished take on a retro CRT hacker terminal. The background should be a deep, rich "void" black (`#090909`) layered with a very subtle, slowly pulsing interlaced CSS scanline overlay and a faint radial vignette to mimic the gentle curve of an old cathode-ray tube monitor. All typography must utilize a crisp, monospaced font like Fira Code, VT323, or JetBrains Mono. The primary text color is a bright, luminous phosphor green (`#00FF41`), with secondary text and technical borders depicted in a muted, darker olive-green (`#008F11`). Links and interactive hover states should snap to a high-contrast terminal cyan (`#00FFFF`) or alert amber (`#FFB000`) to stand out elegantly against the dark background. 

The layout relies heavily upon a dynamic, asymmetric CSS Grid (or masonry layout) to represent an active command-line interface multiplexer, like `tmux`. Upon initial load, the site should visually "boot up," utilizing JavaScript to trigger an accelerated typing animation that prints the layout onto the screen line by line, simulating an automated script execution.

The top of the page acts as the terminal header or boot log. The first visual element is a command-line prompt reading `root@geva-node:~$ whoami`. Immediately beneath it, the developer’s name, GEVA KIPPER, is rendered in large, bold ASCII-art lettering. Below this, a system output line reads, `> CURRENT_PROCESS: Software Engineer creating tools and infrastructure at Google.` Following this is the AI notification, styled as an amber warning banner: `[WARNING: VOLATILE FRONTEND] Initializing daily AI-mutated layout. NOTE: This interface is redesigned and regenerated by AI every 24 hours. The current form is transient.`

The body of the page houses the eight requested content sections. Instead of standard divs, they are styled as individual "terminal panes" or "execution blocks." Each box features a thin, solid green CSS border mimicking ASCII line-drawing characters (┌, ─, ┐, └, ┘) with dark grey backgrounds to give depth. The top border of every box contains an inline title mimicking a file path (e.g., `~/projects/turing_jest.sh`). When a user hovers over any box, the border should glow cyan, and a blinking cursor (`> █`) should appear next to the project title, inviting interaction. Within the boxes, standard links are redesigned as clickable command prompts, styled dynamically (e.g., `[EXECUTE :LINK]`), complete with a glitch or highlighting CSS hover effect.

Here is the exact mapping and content structure for the eight terminal panes the designer must implement:

Box 1 (Header: `~/projects/glglstats.py`): Present the 'GlglStats' project. The description should explain it as a Python-based analysis tool measuring the repetitiveness of Israeli pop-chart songs using LZ77 compression, complete with visualizations. Implement two executable links: one styled as `[HTTPS_GET: glglstats.geekip.dev]` navigating to https://glglstats.geekip.dev, and another styled as `[SYS_VIDPLAY: PyData_TLV]` linking to the YouTube presentation at https://www.youtube.com/watch?v=jrKAtOsj1Lo.

Box 2 (Header: `~/data/ahuzat_dibuk.sh`): Present 'Ahuzat Dibuk'. Describe the Python scraping and data visualization project that captures Tel-Aviv parking lot occupancy statistics and generates heat-maps for locals. Include three media log links styled as `[LOG: CHANNEL_13]`, `[LOG: MAKO]`, and `[LOG: SECRET_TLV]`, linking respectively to the 13tv article, the Mako article, and the Secret Tel-Aviv Facebook post.

Box 3 (Header: `~/algos/2d_orient.cpp`): Present '2D Part Orienting'. Explain it as a C++ implementation in an open-source computational geometry library involving an efficient algorithm to construct a 'push plan' for orienting polygons. Include an executable link styled as `[REPO//CGL_DOCS]` pointing to https://www.cgl.cs.tau.ac.il/projects/2d-part-orienting/.

Box 4 (Header: `~/games/turing_jest.exe`): Present 'The Turing Jest'. Detail it as an LMSYS-style arena where LLMs engage in pun battles, with explanations and punchlines dynamically rated by users. Add a command link styled as `[ENTER_ARENA]` directing to https://jest.geekip.dev.

Box 5 (Header: `~/extensions/rhymes_of_israel.crx`): Present 'Rhymes of Israel'. Describe it as a Chrome extension leveraging LLMs to creatively rewrite Israeli news site headlines into comedic rhymes resembling a highschool yearbook. Embellish the download link as `[INSTALL: CHROME_WEBSTORE]` pointing to the provided Chrome extension URL.

Box 6 (Header: `~/games/hatul_basak.bin`): Present 'Hatul Basak'. Note it as a Hebrew port of the Wikipedia catfishing.net trivia game, featuring both AI-generated daily challenges and user-created themed puzzles. Implement the interactive link as `[INITIALIZE_GAME]` pointing to https://hatulbasak.geekip.dev.

Box 7 (Header: `~/meta/portfolio.cfg`): Present the "Meta Portfolio Website Project." Add a cheeky description noting the recursive nature of reading about the exact website currently being viewed. Include a self-referential loop link styled as `[RECURSION_ROUTINE]` directed back to https://geekip.dev.

Box 8 (Header: `~/network/social_protocols.sys`): Dedicate this pane to external directories. Instead of standard icons, format the links as terminal 'ping' commands in cyan: `> PING github.com/gevak`, `> PING linkedin.com/in/gevakip`, and `> PING facebook.com/geva.kipper`. 

The footer acts as the live system diagnostic monitor, anchored to the bottom of the viewport. The left side should feature the credit: `DAEMON_THREAD: Interface constructed and maintained by Assistant AI Architecture.` The right side of the footer must house a live JavaScript stopwatch styled as server uptime: `SYS_UPTIME: [Tracking passed time since 2026-07-19 02:02:04] - T+ HH:MM:SS`. The elapsed time must tick upwards dynamically in real-time. Cap off the bottom right corner of the footer with a permanent, infinitely blinking lime green terminal cursor block (`█`) to signal that the system is standing by.